<header class="navbar navbar-expand-sm" style="background-color: #f000;">
  <nav class="nav navbar-nav bavbar-left">
    <%= link_to root_path do %>
      <%= image_tag "logo.png",size: "200x100", :class => "logo-img" %>
    <% end %>
    <% if user_signed_in? %>
        <div>
          <%= link_to user_path(current_user.id) do %>
            <%= attachment_image_tag current_user, :profile_image, format:"jpeg", size:"80x80", fallback: "no_image(1).jpg",:class => "head-user-icon"%>
          <% end %>
          <%= search_form_for(q, url: search_bike_path, html: {method: "get"}) do |f| %>
            <%= f.search_field :bike_name_or_bike_name_kana_or_maker_maker_name_or_genre_cont, placeholder: 'メーカー名またはバイク名で検索', class:"form-control" %>
            <%= f.submit "検索", class:"btn btn-default" %>
            <% end %>
        </div>
        <% end %>
      </nav>
      <nav>
        <ul class="nav navbar-nav">
      <div id="sp-menu">
        <h3 class="text-center">MENU</h3>
        <% if user_signed_in? %>
          <li class="hm-link text-center">このサイトについて</li>
          <li class="hm-link text-center">プライバシーポリシー</li>
          <li class="hm-link text-center">お問い合わせ</li>
          <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class:"hm-link text-center" %></li>
        <% else %>
          <li><%= link_to "新規登録", new_user_registration_path,class:"hm-link text-center" %></li>
          <li><%= link_to "ログイン", new_user_session_path, class:"hm-link text-center" %></li>
          <li class="hm-link text-center">このサイトについて</li>
          <li class="hm-link text-center">プライバシーポリシー</li>
          <li class="hm-link text-center">お問い合わせ</li>
        <% end %>
    </div>
    </ul>
  </nav>
    <div id="hm-icon">
   <%= link_to "#", class:"menu-trigger link" do %>
     <span></span>
     <span></span>
     <span></span>
   <% end %>
  </div>
</header>

